// This file is no longer needed but kept for backwards compatibility
// The layout is now handled directly in App.tsx
export function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
